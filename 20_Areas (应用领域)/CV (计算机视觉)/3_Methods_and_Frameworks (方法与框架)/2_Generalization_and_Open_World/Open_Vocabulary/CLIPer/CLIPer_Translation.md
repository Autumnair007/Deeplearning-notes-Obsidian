# CLIPer：分层改进 CLIP 的空间表征以用于开放词汇语义分割

## 摘要

对比语言-图像预训练（Contrastive Language-Image Pre-training，CLIP）在图像级任务上展现出强大的零样本分类能力，这促使研究者在无需训练的情况下将 CLIP 适配到开放词汇语义分割。关键在于改进图像级 CLIP 的空间表征，例如，用自-自注意力图或基于视觉基础模型的注意力图替换最后一层的自注意力图。本文提出一种名为 CLIPer 的新型分层框架，用于分层改进 CLIP 的空间表征。所提出的 CLIPer 包含早层融合和细粒度补偿。我们观察到，早期层的嵌入和注意力图能够保留空间结构信息。受此启发，我们设计了早层融合模块，以生成空间连贯性更好的分割图。随后，我们采用细粒度补偿模块，利用扩散模型的自注意力图补偿局部细节。我们在八个分割数据集上进行了实验。CLIPer 在这些数据集上取得了最先进的性能。使用 ViT-L 和滑动窗口推理时，CLIPer 在 VOC 和 Object 上的 mIoU 分别为 72.2% 和 44.7%，比 ProxyCLIP 高 11.6% 和 5.5%。我们的代码可在 https://github.com/linsun449/cliper.code 获取。

## 1. 引言

开放词汇语义分割 [2, 50, 52] 旨在将一幅图像划分为不同的组，并为每组分配一个属于任意类别的标签。近年来，研究者主要探索了使用视觉-语言模型进行开放词汇语义分割。

对比语言-图像预训练（CLIP）模型 [26] 由于在大规模图像-文本配对数据 [31] 上进行预训练，已经在图像级分类任务上展现出强大的零样本能力。在此基础上，已有多种方法被提出，以便在无需训练的情况下将 CLIP 适配到开放词汇语义分割。关键挑战是改进图像级监督模型的空间表征，使其可用于像素级分割。如图 1(a) 所示，一些方法用自-自注意力图修改最后一层原始的自注意力图，从而更好地保持局部空间信息。例如，MaskCLIP [52] 使用恒等自-自矩阵作为最后一层的自注意力图来生成视觉图像块嵌入，而 SCLIP [36] 和 ClearCLIP [16] 则使用查询到查询或键到键注意力图替换原始自注意力图。ProxyCLIP [15] 不使用原始自注意力图或自-自注意力图，而是从视觉基础模型（vision foundation model，VFM）[4] 中提取自注意力图，并将其用作图 1(b) 中最后一层的自注意力图。这些方法在无需额外训练的情况下增强了 CLIP 在开放词汇设定下的分割性能。然而，这些方法主要考虑改进 CLIP 最后一层的自注意力图。

![[../../../../../../99_Assets (资源文件)/images/CLIPeR_Figure_1.png]]

**图 1. 与基于 CLIP 的无需训练开放词汇语义分割方法的比较。** 在 (a) 中，方法 [10, 16, 36] 用自-自注意力替换最后一层原始的自注意力，这能够更好地保持空间连贯性。在 (b) 中，ProxyCLIP [15] 选择了另一种策略，用基于视觉基础模型（VFM）的注意力图替换原始自注意力图。在 (c) 中，我们利用早期层的嵌入和自注意力图，充分挖掘 CLIP 中的空间信息。随后，我们使用扩散模型进行细粒度补偿，将其作为后处理来改善局部细节。

**图片内容解释：** 图中依次对比了三条流程：(a) 图像编码器通过自-自注意力产生分割图；(b) 图像编码器使用 VFM 注意力产生分割图；(c) CLIPer 将图像编码器的早层信息送入早层融合，先得到粗分割图，再经细粒度补偿输出结果。三条流程均使用文本编码器提供类别文本表征。

本文聚焦于两个因素，以分层改进空间表征。(i) 第一个因素是改进图像块级空间连贯性。我们观察到，早期层与最后一层共享相似的嵌入空间，同时也具有良好的空间连贯性。这使得无需训练的早层信息特征融合能够用于改进分割。(ii) 第二个因素是细粒度补偿。图像与文本之间的图像块级相似度图在局部细节上相对粗糙。为了改善分割，有必要进一步改进局部细节。

基于上述两个因素，我们提出了一种名为 CLIPer 的新型分层开放词汇语义分割方法。CLIPer 由早层融合模块和细粒度补偿模块组成。如图 1(c) 所示，早层融合模块整合早期层的图像块嵌入和注意力图，以改善输出图像块嵌入的空间连贯性。基于输出的图像块嵌入和任意类别的文本嵌入，我们可以生成粗分割图。随后，细粒度补偿模块整合 Stable Diffusion 的精细空间信息，以补偿局部细节。我们在多个分割数据集上进行了实验。贡献和优点概括如下：

- 我们提出了一种新型的、基于 CLIP 的无需训练方法，该方法分层改进 CLIP 的空间表征，以用于开放词汇语义分割。

- 我们提出了一种早层融合策略，通过整合早层信息来改善 CLIP 内部的图像块级连贯性。

- 细粒度补偿模块利用扩散模型中的精细细节信息，对 CLIP 中丢失的局部细节进行细化，从而在无需任何训练的情况下实现更精确的分割。

- 我们的方法在八个数据集上具有更优的性能。使用 ViT-L 和滑动窗口推理时，其平均得分为 48.6%，比 ProxyCLIP 高 5.7%。

## 2. 相关工作

### 视觉-语言预训练模型

视觉-语言预训练模型旨在建立图像与文本之间的关系。在这些模型中，CLIP [26] 是最成功的视觉-语言模型之一，它在超大规模图像-文本配对数据集上进行训练。得益于大规模预训练，CLIP 在多种图像级任务上展现出强大的零样本分类性能。OpenCLIP [7] 通过对缩放定律进行全面的实验分析来探索如何改进 CLIP。为应对图像-文本标注成本高昂的挑战，ALIGN [13] 提出使用大规模含噪图像-文本数据进行模型学习。

### 开放词汇语义分割

与传统语义分割 [23, 33, 37] 不同，开放词汇语义分割 [21, 27, 32] 对属于任意类别的对象进行分割。相关方法可分为基于训练的方法 [5, 24, 38, 46] 和无需训练的方法 [22, 36]。基于训练的方法首先在给定训练数据集的一组固定类别上训练模型，然后应用所学模型分割来自任意语义类别的对象。一些基于训练的方法 [20, 47] 遵循两阶段流水线，其中第一阶段提取掩码候选，第二阶段为掩码候选分配语义标签。例如，OVSeg [20] 首先使用基于查询的框架 Mask2Former [6] 训练类别无关的掩码候选，然后微调 CLIP，对裁剪和掩码后的图像进行分类。一些基于训练的方法采用单阶段流水线。例如，SAN [49] 引入侧适配器，使 CLIP 同时适用于分类和分割。CAT-Seg [8] 构建像素级代价图用于分割。SED [42] 引入一种带有类别提前拒绝机制的简单编码器-解码器架构，以实现高效分割。Cascade-CLIP [18] 使用多个独立的文本-图像解码器从不同层提取特征，引入了额外的可学习网络。

与基于训练的方法相比，无需训练的方法旨在不进行任何训练，直接将视觉-语言模型适配到开放词汇语义分割。大多数无需训练的方法聚焦于改善图像级监督 CLIP 的空间连贯性。例如，MaskCLIP [52] 移除最后一层的自注意力，并直接使用值嵌入作为输出嵌入来执行像素级分割。SCLIP [36] 和 ClearCLIP [16] 并不移除自注意力，而是使用查询到查询或键到键注意力图替换最后一层的原始注意力图。CaR [35] 采用循环框架逐步增强分割。此外，一些研究者探索了使用扩散模型进行开放词汇语义分割。例如，ODISE [47] 使用扩散模型生成掩码候选，并生成掩码的视觉嵌入用于分类。OVDiff [14] 使用扩散模型生成任意类别的支持图像，并提取原型特征来分割推理图像。DiffSegmenter [39] 和 iSeg [34] 利用扩散模型中的自注意力图和交叉注意力图进行开放词汇分割。

近期基于 DINO 的方法 [15, 40] 聚焦于改进 CLIP 的空间信息。例如，CLIP-DINOiser [40] 使用具有良好空间属性的 DINO 特征，在训练期间引导 CLIP 特征学习，而 ProxyCLIP [15] 则用 DINO 中的代理注意力替换 CLIP 中的原始自注意力。我们的方法与这些方法相关，但存在显著差异。我们不通过训练引导整合 DINO 特征的空间连贯性，也不进行自注意力替换，而是将扩散模型用作后处理步骤，以细化粗分割，并显著优于这些采用 DINO 的方法。此外，我们的早层融合策略能够充分挖掘 CLIP 内部的空间信息。

## 3. 方法

### 3.1 预备知识

**CLIP。** CLIP [26] 包含一个图像编码器和一个文本编码器。图像编码器包含一系列 Transformer 块 [11]，输入图像被划分为图像块，并依次通过这些块进行处理。Transformer 块包含残差注意力和残差 FFN。首先，添加一个类别词元，以聚合来自所有图像块的信息，形成图像的全局表征。随后，每个 Transformer 块处理输入嵌入 $F=[F_{cls},F_1,\ldots,F_{hw}]$，其中 $F_{cls}$ 表示类别词元的嵌入，其余项表示图像块词元的嵌入。类别词元的输出嵌入最终与文本编码器的嵌入对齐。

**Stable Diffusion。** 图像扩散模型从随机高斯噪声出发，通过一系列去噪步骤生成图像。通过在大规模数据集上训练，扩散模型 Stable Diffusion [29] 能够生成具有丰富细节的高质量图像。已有研究表明，Stable Diffusion 中的特征能够准确捕获局部细节信息。因此，我们探索使用 Stable Diffusion 改善分割的局部细节。

**注意力机制。** CLIP 和 Stable Diffusion 都利用注意力机制。CLIP 使用自注意力对图像块之间的关系进行建模。相比之下，Stable Diffusion 同时包含自注意力和交叉注意力，其中自注意力提取图像内部的空间连贯性，交叉注意力则使模型能够整合条件信息（例如文本描述），以引导图像生成。无论是自注意力还是交叉注意力，其输出均由查询 $Q$、键 $K$ 和值 $V$ 计算如下：

$$
\operatorname{Att}(Q,K,V)=A\times V,
\tag{1}
$$

注意力图 $A$ 定义为：

$$
A=\operatorname{Softmax}\left(\frac{QK^{T}}{\sqrt{d}}\right),
\tag{2}
$$

其中，$d$ 为特征维度。在自注意力中，查询、键和值来自同一组图像嵌入。相比之下，在交叉注意力中，查询来自图像，而键和值来自文本描述。

### 3.2 动机

为了将预训练 CLIP 适配到开放词汇分割，一种直接的方法是丢弃类别词元，仅使用图像块词元与文本嵌入生成像素级相似度图。然而，由于 CLIP 是在图像级分类任务上预训练的，这种简单方法通常会因图像块嵌入的空间连贯性较弱而取得较差的分割结果。为解决这一问题，一些方法 [10, 36] 主要聚焦于修改最后一层，以改善空间连贯性。相比之下，受两个方面观察结果的启发，我们提出分层改进空间表征，以获得更好的分割。

第一，早期层的嵌入可用于改善空间连贯性。首先，如图 2(a) 所示，与最后一层相似，早期层的图像块嵌入也保留了空间信息。其次，在图 2(b) 中，早层嵌入与最后一层的嵌入具有相似性。这种相似性使早层信息和晚层信息能够在无需训练的情况下进行有效融合。

![[../../../../../../99_Assets (资源文件)/images/CLIPeR_Figure_2.png]]

**图 2. CLIP 中图像块嵌入的可视化。** 在 (a) 中，我们使用 t-SNE 对嵌入进行可视化，其中早期嵌入和后期嵌入都表现出良好的空间连贯性。在 (b) 中，我们评估了早期层在某一特定点处的嵌入与最后一层嵌入之间的余弦相似度，结果表明早期嵌入和最后一层嵌入共享相似的嵌入空间。

**图片内容解释：** 两组示例均按层展示图像块嵌入。(a) 行用不同颜色表示 t-SNE 空间中的嵌入分布，空间上属于同一对象的区域在多层中仍形成连贯结构；(b) 行以红星位置为查询点展示其与最后一层嵌入的余弦相似度，黄色高响应区域随层数变化逐渐对应到目标对象。

第二，预训练 Stable Diffusion（SD）能够生成具有丰富细节的高质量图像。如图 3 所示，通过可视化 CLIP 和 SD 中的自注意力图，我们观察到 SD 的注意力图能够有效捕获局部细节。这与 CLIP 中通常响应更宽泛语义区域的注意力图形成对比。这一关键观察表明，我们能够整合来自 SD 的精细空间信息，以改善 CLIP 生成的粗分割。

![[../../../../../../99_Assets (资源文件)/images/CLIPeR_Figure_3.png]]

**图 3. CLIP 和 Stable Diffusion（SD）中自注意力图的可视化。** 我们展示了 CLIP 和 SD 在选定位置处的自注意力图。与 CLIP 相比，SD 的自注意力图更加聚焦于捕获局部细节。

**图片内容解释：** 图中以鸟和两只猫为例，虚线箭头标出所选查询位置。CLIP 的响应覆盖较宽的对象区域，而 SD 的响应更集中于鸟的喙、身体局部、窗框边缘或单只猫等更局部的结构。

### 3.3 框架

**概述。** 受上述动机和见解启发，我们提出了一种用于开放词汇语义分割的新型分层方法。图 4 展示了所提出方法 CLIPer 的整体架构。CLIPer 由两个互补组件组成。在第一个组件中，我们利用早层融合模块生成具有更好空间连贯性的图像块嵌入，然后根据图像块嵌入与文本编码器所生成文本嵌入之间的相似度，生成粗分割图。在第二个组件中，我们使用 Stable Diffusion 的注意力图执行细粒度补偿。

![[../../../../../../99_Assets (资源文件)/images/CLIPeR_Figure_4.png]]

**图 4. CLIPer 方法的整体架构。** CLIPer 包含两个组件：早层融合和细粒度补偿。在早层融合中，我们聚合 CLIP 图像编码器的早层信息，包括嵌入和注意力图，以改善输出嵌入的空间连贯性；这些输出嵌入与文本嵌入一起用于生成粗分割图。细粒度补偿旨在使用 Stable Diffusion 的自注意力图细化粗分割图的局部细节。

**图片内容解释：** 下半部分为早层融合：输入图像依次通过多个 CLIP 块，汇集各早层的图像块嵌入 $F$ 与注意力图，并通过平均得到 $A_{avg}$；最后一层经 LayerNorm、V-Projection 和 Projection 后，与类别文本（如 Chair、Table、Pottedplant）进行相似度计算与融合，生成粗分割 $S_c$。上半部分为细粒度补偿：同一图像进入 SD，SFSA 模块产生融合注意力 $A_f$，与 $S_c$ 结合后输出细粒度分割结果。

**早层融合。** 该模块旨在使用早期层的嵌入改善空间连贯性。具体而言，给定一幅图像，我们首先将图像划分为图像块嵌入 $F^0\in\mathbb{R}^{(hw+1)\times D}$，然后将这些嵌入送入一系列 Transformer 块。对于第 $n$ 个 Transformer 块，我们生成查询 $Q^n$、键 $K^n$ 和值 $V^n$，即 $Q^n=\operatorname{Proj}_q(\operatorname{LN}(F^{n-1}))$、$K^n=\operatorname{Proj}_k(\operatorname{LN}(F^{n-1}))$、$V^n=\operatorname{Proj}_v(\operatorname{LN}(F^{n-1}))$。这里，$\operatorname{LN}$ 表示层归一化，投影 $\operatorname{Proj}_q$、$\operatorname{Proj}_k$ 和 $\operatorname{Proj}_v$ 分别表示一个线性层。利用查询 $Q^n$、键 $K^n$ 和值 $V^n$，第 $n$ 个 Transformer 块的输出嵌入 $F^n$ 计算为：

$$
\bar{F}^{n}=\operatorname{Att}(Q^n,K^n,V^n)+F^{n-1},
\tag{3}
$$

$$
F^n=\operatorname{FFN}(\operatorname{LN}(\bar{F}^{n}))+\bar{F}^{n},
\tag{4}
$$

其中，FFN 表示前馈网络。第 $n$ 个 Transformer 的注意力图在式 (3) 的注意力操作中生成，记为 $A^n$。

类似地，我们能够生成直至倒数第二层的所有 Transformer 块的嵌入和注意力图，并将它们记为两个集合：$\mathcal{F}=\{F^i\mid i=1,2,\ldots,N-1\}$ 和 $\mathcal{A}=\{A^i\mid i=1,2,\ldots,N-1\}$。我们首先生成一个平均注意力图：

$$
A_{avg}=\frac{1}{N}\sum_{n=1}^{N-1}A^n.
\tag{5}
$$

然后，我们用平均注意力图 $A_{avg}$ 替换最后一层原始的自注意力图，并将所有嵌入送入最后一层。与 ClearCLIP [16] 类似，我们省略最后一个 Transformer 块中的前馈网络和残差连接，这能够简化表征，同时更好地与文本嵌入对齐。由此，我们为不同层生成多个输出嵌入。

最后，我们计算多个输出嵌入与 CLIP 文本编码器所产生文本嵌入之间的余弦相似度，并计算平均相似度图。通过将每个图像块嵌入映射到候选类别嵌入，该平均相似度图被用作粗分割。

**细粒度补偿。** CLIP 生成的图像块级分割仍然相对粗糙，从而限制了分割精度。为了更好地补偿粗分割图的局部细节，我们采用自融合自注意力（self-fused self-attention，SFSA）来利用 Stable Diffusion 的自注意力图，因为我们发现这种注意力能够有效捕获细粒度局部信息。这种保持局部性的特征对于细化图像块级分割的空间细节非常有益。

具体而言，我们首先将图像连同一个空（null）文本提示送入 Stable Diffusion，在最高空间分辨率处获得对应的多头自注意力图。我们将这些注意力图记为 $A_m\in\mathbb{R}^{H\times L\times L}$，其中 $H$ 表示注意力头的数量，$L$ 表示 Stable Diffusion 中特征图的空间大小。然后，我们通过跨注意力头的矩阵链式乘法融合这些注意力图 $A_m$，其形式为：

$$
A_f=A_m[0]\times A_m[1]\times\cdots\times A_m[H-1],
\tag{6}
$$

其中，$A_m[i]$ 表示第 $i$ 个头的自注意力图。随后，我们使用融合后的注意力图 $A_f$ 对上采样后的粗分割图 $S_c$ 进行细化：

$$
S_f=A_f\times S_c.
\tag{7}
$$

最后，我们将 $S_f$ 上采样到输入图像的分辨率，得到细粒度像素级分割图。

## 4. 实验

### 4.1 实验设置

**数据集。** 我们在八个数据集上评估 CLIPer：(1) 考虑背景类别。我们使用 PASCAL VOC（VOC）[12]、PASCAL Context（Context）[25] 和 COCO Object（Object）[3]；(2) 不考虑背景类别。我们使用 PASCAL VOC（VOC20）、PASCAL Context（Context59）、COCO-Stuff（Stuff）[3]、Cityscapes [9] 和 ADE20K（ADE）[51]。在性能评估中，我们使用每个数据集的验证集。此外，对于弱监督语义分割，我们在 VOC 和 COCO 数据集的训练集上评估伪掩码生成性能。

**指标。** 我们使用平均交并比（mean Intersection over Union，mIoU）评估分割，并采用 mAP、F1 分数、精确率（Precision，P）和召回率（Recall，R）评估图像级分类。

**实现细节。** 我们在一张具有 24G 显存的 RTX 3090 上实现该方法。我们采用 ViT-B 和 ViT-L 作为骨干网络，并使用 Stable Diffusion V2.1 [29] 进行细粒度补偿。在 Stable Diffusion 中，我们从总计 50 个时间步中的第 45 个时间步提取注意力图。我们设置了包含类别描述的文本提示，与 SCLIP [36] 和 ProxyCLIP [15] 类似。与 ProxyCLIP [15] 类似，我们在保持原始宽高比的同时，将所有输入图像的短边缩放到 336 像素。为了效率，我们将整幅图像直接送入 CLIP 图像编码器，同时也报告了使用类似于 [52]、[36]、[16] 和 [15] 的滑动窗口策略所得到的结果。

### 4.2 与其他方法的比较

**mIoU。** 表 1 将我们的方法与多个数据集上的一些最先进方法进行比较。使用 ViT-B 和 ViT-L 骨干网络时，我们的方法在所有这些数据集上的平均性能几乎都是最佳的。例如，在使用 ViT-L 骨干网络的 VOC 上，SCLIP [36] 的 mIoU 为 43.5%，ProxyCLIP [15] 的 mIoU 为 60.6%，而我们的方法在不采用滑动窗口策略时取得了 69.8% 的 mIoU。也就是说，我们的方法在 VOC 上分别比 SCLIP 和 ProxyCLIP 高 26.3% 和 9.2%。在使用 ViT-L 骨干网络的 ADE 上，ClearCLIP [16] 和 ProxyCLIP 的 mIoU 分别为 15.0% 和 22.6%，而我们的方法取得了 24.4% 的 mIoU。也就是说，我们的方法在 ADE 上分别提升了 9.4% 和 1.8%。使用滑动窗口推理时，其性能更好。

**表 1. 与现有开放词汇分割方法的比较。** 对于无需训练的方法，‡ 表示使用滑动窗口推理。在无需训练的方法中，即使不使用滑动窗口推理，我们的方法也具有最佳的平均性能。

| 类型 | 方法 | 编码器 | VOC | Context | Object | VOC20 | Context59 | Stuff | Cityscapes | ADE | 平均 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 基于训练（弱监督） | SegCLIP [24] | ViT-B/16 | 52.6 | 24.7 | 26.5 | - | - | - | - | - | - |
|  | ViewCo [28] | ViT-S/16 | 52.4 | 23.0 | 23.5 | - | - | - | - | - | - |
|  | OVSegmentor [46] | ViT-B/16 | 53.8 | 20.4 | 25.1 | - | - | - | - | - | - |
|  | CoCu [44] | ViT-S/16 | 51.4 | 23.6 | 22.7 | - | - | 15.2 | 22.1 | 12.3 | - |
|  | GroupViT [45] | ViT-S/16 | 50.4 | 18.7 | 27.5 | 79.7 | 23.4 | 15.3 | 11.1 | 9.2 | 29.4 |
|  | TCL [5] | ViT-B/16 | 51.2 | 24.3 | 30.4 | 77.5 | 30.3 | 19.6 | 23.1 | 14.9 | 33.9 |
|  | CLIP-DINOiser [40] | ViT-B/16 | 62.2 | 32.4 | 35.0 | 80.9 | 35.9 | 24.6 | 31.7 | 20.0 | 40.3 |
| 无需训练 | CLIP‡ [26] | ViT-B/16 | 16.4 | 8.4 | 5.6 | 41.9 | 9.2 | 4.4 | 5.5 | 2.9 | 11.8 |
|  | CLIPSurgery [19] | ViT-B/16 | - | 29.3 | - | - | - | 21.9 | 31.4 | - | - |
|  | MaskCLIP‡ [52] | ViT-B/16 | 38.8 | 23.6 | 20.6 | 74.9 | 26.4 | 16.4 | 12.6 | 9.8 | 27.9 |
|  | SCLIP‡ [36] | ViT-B/16 | 59.1 | 30.4 | 30.5 | 80.4 | 34.2 | 22.4 | 32.2 | 16.1 | 38.2 |
|  | ClearCLIP‡ [16] | ViT-B/16 | 51.8 | 32.6 | 33.0 | 80.9 | 35.9 | 23.9 | 30.0 | 16.7 | 38.1 |
|  | ProxyCLIP‡ [15] | ViT-B/16 | 61.3 | 35.3 | 37.5 | 80.3 | 39.1 | 26.5 | 38.1 | 20.2 | 42.3 |
|  | **CLIPer（本文）** | ViT-B/16 | **65.9** | **37.6** | **39.0** | **85.2** | **41.7** | **27.5** | **38.3** | **21.4** | **44.4** |
|  | **CLIPer‡（本文）** | ViT-B/16 | **66.5** | **38.3** | **40.0** | **86.0** | **42.4** | **28.6** | **38.7** | **22.0** | **45.3** |
| 无需训练 | CLIP‡ [26] | ViT-L/14 | 8.2 | 4.1 | 2.7 | 15.6 | 4.4 | 2.4 | 2.5 | 1.7 | 5.2 |
|  | MaskCLIP‡ [52] | ViT-L/14 | 23.3 | 11.7 | 7.2 | 29.4 | 12.4 | 8.8 | 11.5 | 7.2 | 13.9 |
|  | SCLIP‡ [36] | ViT-L/14 | 43.5 | 22.3 | 25.0 | 69.1 | 25.2 | 17.6 | 18.6 | 10.9 | 29.0 |
|  | ClearCLIP‡ [16] | ViT-L/14 | 46.1 | 26.7 | 30.1 | 80.0 | 29.6 | 19.9 | 27.9 | 15.0 | 34.4 |
|  | CaR [35] | ViT-L/14 | 67.6 | 30.5 | 36.6 | **91.4** | 39.5 | - | - | 17.7 | - |
|  | ProxyCLIP‡ [15] | ViT-L/14 | 60.6 | 34.5 | 39.2 | 83.2 | 37.7 | 25.6 | 40.1 | 22.6 | 42.9 |
|  | ProxyCLIP‡ [15] | ViT-H/14 | 65.0 | 35.4 | 38.6 | 83.3 | 39.6 | 26.8 | 42.0 | 24.2 | 44.4 |
|  | **CLIPer（本文）** | ViT-L/14 | **69.8** | **38.0** | **43.3** | **90.0** | **43.6** | **28.7** | **41.6** | **24.4** | **47.3** |
|  | **CLIPer‡（本文）** | ViT-L/14 | **72.2** | **39.5** | **44.7** | **89.8** | **44.6** | **30.4** | **42.5** | **25.0** | **48.6** |

**类别分类与掩码预测。** 开放词汇语义分割可以被视为两个方面：类别分类和掩码预测。为了深入展示所提出方法在这两个方面的优势，我们通过两个实验提供了与其他方法的更多比较。(i) 我们给出图像级精确率（P）和召回率（R）的比较，以展示识别图像中类别的优势。(ii) 我们在给定图像级类别标签时比较分割精度，以展示掩码预测的优势。弱监督语义分割旨在根据训练集的图像级类别标签训练模型。通过将我们的方法与弱监督方法进行比较，可以展示我们的方法在掩码预测方面的优势。

在表 2 中，我们计算了所有方法的图像级分类得分，并报告 mAP、F1、P 和 R。我们的方法在所有这些指标上都取得了最佳性能。我们还观察到使用 ViT-B 时有相似的提升。这表明我们的方法在类别分类方面表现更好，而这对开放词汇语义分割非常有用。

**表 2. 与现有方法在图像级类别分类能力上的比较。** 我们通过对分割图进行最大池化来计算不同类别的分类得分，然后计算 mAP、F1、精确率（P）和召回率（R）。我们的方法在所有数据集上均取得最佳结果，表明其具有更优的类别分类性能。

| 方法 | 编码器 | VOC mAP | VOC F1 | VOC P | VOC R | Context mAP | Context F1 | Context P | Context R | Object mAP | Object F1 | Object P | Object R |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| MaskCLIP [52] | ViT-L/14 | 87.3 | 63.2 | 56.3 | 72.1 | 58.6 | 48.9 | 48.5 | 49.3 | 67.4 | 48.2 | 47.6 | 52.4 |
| SCLIP [36] | ViT-L/14 | 92.6 | 77.3 | 85.5 | 70.5 | 61.1 | 54.8 | 61.9 | 49.1 | 74.4 | 56.6 | 66.6 | 49.2 |
| ClearCLIP [16] | ViT-L/14 | 91.7 | 76.1 | 85.7 | 68.5 | 60.9 | 54.9 | 52.8 | 57.2 | 73.4 | 53.5 | 61.3 | 47.5 |
| ProxyCLIP [15] | ViT-L/14 | 94.0 | 77.6 | 84.5 | 71.7 | 64.1 | 56.1 | 53.7 | 58.8 | 77.7 | 60.5 | 68.2 | 54.3 |
| **CLIPer（本文）** | ViT-L/14 | **94.6** | **86.0** | **86.7** | **85.3** | **68.9** | **63.3** | **63.4** | **64.3** | **77.9** | **62.3** | **69.7** | **56.3** |

表 3 进一步将我们的方法与用于伪掩码生成的弱监督语义分割方法进行了比较，其中给定了图像级类别标签。与这些方法相比，我们的方法取得了最佳性能。例如，我们的方法分别比 CLIP-ES [21] 和 iSeg [34] 高 6.1% 和 1.7%。这表明我们的方法能够改善掩码预测，而掩码预测对于开放词汇语义分割非常重要。

**表 3. 与弱监督语义分割方法的比较。** 我们的方法和这些弱监督方法都根据给定的图像级类别标签预测伪掩码。CLIPer 取得了最佳结果，表明我们的方法在掩码预测上具有更好的结果。

| 类型 | 方法 | VOC | COCO |
|---|---|---:|---:|
| 基于训练 | IRN [1] | 66.5 | 42.4 |
|  | AdvCAM [17] | 55.6 | 35.8 |
|  | MCTformer [48] | 61.7 | - |
|  | ToCo [30] | 72.2 | - |
|  | CLIMS [43] | 56.6 | - |
| 无需训练 | CLIP-ES [21] | 70.8 | 39.7 |
|  | DiffSegmenter [39] | 70.5 | - |
|  | T2M [41] | 72.7 | 43.7 |
|  | iSeg [34] | 75.2 | 45.5 |
|  | **CLIPer（本文）** | **76.9** | **47.3** |

**推理时间。** 表 4 比较了推理时间和准确率。我们给出多个版本以满足不同需求。与 ClearCLIP [16] 相比，CLIPer*（不使用细粒度补偿）的速度更快、mIoU 更高。与 ProxyCLIP [15] 相比，CLIPer* 的速度更快，mIoU 相当。带有细粒度补偿的 CLIPer 进一步改进了 CLIPer*。

**表 4. VOC 上 mIoU 与推理时间的比较。** * 表示不使用细粒度补偿，‡ 表示滑动窗口推理。CLIPer* 具有最快的速度，而 CLIPer 和 CLIPer‡ 具有最佳性能。

| 方法 | 编码器 | 输入尺寸 | 时间（ms）↓ | mIoU ↑ |
|---|---|---:|---:|---:|
| ClearCLIP‡ [16] | ViT-B/16 | $\sim448\times624$ | 22 | 51.8 |
| ProxyCLIP‡ [15] | ViT-B/16 | $\sim336\times468$ | 82 | 61.3 |
| **CLIPer*（本文）** | ViT-B/16 | $\sim336\times468$ | **14** | 60.1 |
| **CLIPer（本文）** | ViT-B/16 | $\sim336\times468$ | 158 | 65.9 |
| **CLIPer‡（本文）** | ViT-B/16 | $\sim336\times468$ | 186 | **66.5** |
| ClearCLIP‡ [16] | ViT-L/14 | $\sim448\times624$ | 68 | 46.1 |
| ProxyCLIP‡ [15] | ViT-L/14 | $\sim336\times468$ | 105 | 60.6 |
| **CLIPer*（本文）** | ViT-L/14 | $\sim336\times468$ | **47** | 61.2 |
| **CLIPer（本文）** | ViT-L/14 | $\sim336\times468$ | 192 | 69.8 |
| **CLIPer‡（本文）** | ViT-L/14 | $\sim336\times468$ | 257 | **72.2** |

**定性结果。** 图 5 展示了在 VOC、Context 和 Object 上进行定性比较的一些示例。与这些方法 [10, 15, 16] 相比，所提出的方法具有更准确的分割图和更精确的分类。例如，我们的方法在第二列中对自行车的分割更精细，并正确分类了人物，在第五列中准确分割了沙发。

![[../../../../../../99_Assets (资源文件)/images/CLIPeR_Figure_5.png]]

**图 5. 与现有方法的定性比较。** 我们展示了三个不同数据集上的分割结果。与这些方法相比，我们的方法具有更准确、更接近真实标注的分割结果。

**图片内容解释：** 图中按列展示 VOC、Context 和 Object 的输入图像，按行依次展示 MaskCLIP、SCLIP、ClearCLIP、ProxyCLIP、本文方法和真实标注。本文方法在人物、自行车、动物和家具等区域上呈现出更完整的对象范围及更少的零散误分区域。

### 4.3 消融研究

**不同模块的影响。** 表 5 展示了整合不同模块的结果。基线用值到值注意力图替换最后一层原始的自注意力图，并移除前馈网络（FFN）和残差连接。基线在 VOC、Context 和 Object 上的 mIoU 分别为 51.2%、26.5% 和 32.3%。添加早层融合（ELF）模块后，其在 VOC、Context 和 Object 上的 mIoU 分别为 61.2%、34.3% 和 39.6%，分别比基线高 10.0%、7.8% 和 7.3%。仅使用细粒度补偿（FGC）模块时，其分别比基线高 11.6%、3.2% 和 4.1%。同时整合 ELF 和 FGC 模块后，在三个数据集上分别总计提升 18.6%、11.5% 和 11.0%。这有力地表明，所提出的模块能够改进开放词汇分割性能。

**表 5. CLIPer 中不同模块的消融研究。** ELF 表示早层融合，FGC 表示细粒度补偿。所提出的模块能够显著改进基线的性能。

| ELF | FGC | VOC | Context | Object |
|:---:|:---:|---:|---:|---:|
| ✗ | ✗ | 51.2 | 26.5 | 32.3 |
| ✓ | ✗ | 61.2 | 34.3 | 39.6 |
| ✗ | ✓ | 62.8 | 29.7 | 36.4 |
| ✓ | ✓ | **69.8** | **38.0** | **43.3** |

**早层融合的效果。** 表 6 将我们的早层融合与一些自-自注意力操作进行了比较。早层融合模块同时融合早期层的图像块嵌入和注意力图。在上半部分，我们将早层融合注意力与这些自-自注意力操作进行比较，所有这些操作都以最后一层的嵌入作为输入。与这些自-自注意力操作相比，早层融合注意力具有最佳性能。例如，在 VOC、Context 和 Object 上，查询到查询注意力的 mIoU 分别为 50.1%、22.5% 和 28.7%，而早层融合注意力的 mIoU 分别为 58.6%、33.1% 和 38.4%。也就是说，早层融合注意力分别比查询到查询注意力高 8.5%、10.6% 和 9.7%。

**表 6. 早层融合中不同设计的影响。** 早层融合包含对嵌入和注意力图的融合。在上半部分，我们将融合注意力与一些自-自注意力操作进行比较。在下半部分，我们将早层嵌入送入最后一层的注意力图。

| 注意力类型 | 早层嵌入 | VOC | Context | Object |
|---|:---:|---:|---:|---:|
| 查询-查询 | ✗ | 50.1 | 22.5 | 28.7 |
| 键-键 | ✗ | 44.8 | 25.4 | 27.0 |
| 值-值 | ✗ | 51.2 | 26.5 | 32.3 |
| 恒等矩阵 | ✗ | 40.1 | 22.0 | 25.7 |
| **早层注意力** | ✗ | **58.6** | **33.1** | **38.4** |
| 查询-查询 | ✓ | 55.7 | 26.9 | 32.0 |
| 键-键 | ✓ | 53.2 | 29.5 | 33.1 |
| 值-值 | ✓ | 54.8 | 29.4 | 34.4 |
| 恒等矩阵 | ✓ | 43.3 | 24.8 | 27.8 |
| **早层注意力** | ✓ | **61.2** | **34.3** | **39.6** |

在下半部分，我们展示了将早层嵌入送入最后一层的影响。将早层嵌入整合到早层注意力中时，在 VOC、Context 和 Object 上分别提升 2.6%、1.2% 和 1.2%。我们观察到，使用早层嵌入也能够改善现有自-自注意力操作的性能。例如，将其与值到值注意力结合时，在 VOC、Context 和 Object 上分别提升 3.6%、2.9% 和 2.1%。

**细粒度补偿的效果。** 表 7 展示了融合 Stable Diffusion（SD）中多头注意力图的不同策略，包括单个头（Single）、对所有头取平均（Mean），以及通过矩阵乘法组合所有头（Multiplication）。与基线相比，三种策略都能够改善性能，这表明 SD 中的注意力图能够改善基于 CLIP 的分割。在这些策略中，矩阵乘法具有最佳性能，因此被采用为最终设定。

**表 7. 使用 Stable Diffusion 多头注意力图时不同策略的比较。** Single 表示我们评估每个单独的头并报告表现最佳的头。Mean 表示对多头注意力图取平均，Multiplication 表示执行矩阵乘法以融合多头注意力图。

| 类型 | VOC | Context | Object |
|---|---:|---:|---:|
| Single | 65.3 | 36.1 | 41.2 |
| Mean | 64.9 | 36.1 | 41.0 |
| **Multiplication** | **69.8** | **38.0** | **43.3** |

表 8 展示了使用不同模型进行补偿的效果。更详细的比较可在补充材料中找到。DINO 和扩散模型都能够改善分割，而扩散模型取得了更好的性能。原因是扩散模型主要聚焦于图像生成，通过大规模预训练自然地更擅长处理局部细节。

**表 8. 与代理注意力的比较。** 我们在 FGC 中用代理注意力替换扩散模型。

| 策略 | VOC | Context | Object |
|---|---:|---:|---:|
| 本文 ELF（即 CLIPer*） | 61.2 | 34.3 | 39.6 |
| 使用来自 DINO 的注意力 | 68.2 | 35.8 | 41.9 |
| **使用来自扩散模型的注意力** | **69.8** | **38.0** | **43.3** |

**定性比较。** 图 6 展示了细粒度补偿前后的一些示例。在细粒度补偿之前，(b) 中的注意力图提供了对象的粗略空间结构信息。使用细粒度补偿后，(c) 中的注意力图在对象轮廓周围提供了更准确的响应。因此，与 (d) 相比，使用细粒度补偿的 (e) 具有更准确的分割图。这表明细粒度补偿能够改善粗分割图的局部细节。

![[../../../../../../99_Assets (资源文件)/images/CLIPeR_Figure_6.png]]

**图 6. 细粒度补偿的可视化。** 给定顶部的输入图像，我们在 (a) 和 (b) 中展示细粒度补偿前后的注意力图，在 (c) 和 (d) 中展示二值分割图。我们还在 (e) 中展示真实分割图。

**图片内容解释：** 四列分别对应飞机、跳跃人物、舞台人物和动物图像。(a) 与 (b) 展示补偿前后从弥散响应到更清晰局部轮廓的变化；(c) 与 (d) 展示对应二值分割结果；(e) 给出真实标注以供对照。

## 5. 结论

本文提出 CLIPer，这是一种新型无需训练的方法，用于分层改进 CLIP 的空间表征，以进行开放词汇语义分割。为实现这一目标，我们设计了两个组件，包括早层融合和细粒度补偿。早层融合旨在利用图像块嵌入和注意力图的早层信息，改善输出图像块嵌入的空间连贯性。细粒度补偿模块使用扩散模型的精细注意力图，进一步改善分割图的局部细节。我们的方法在多个公开数据集上取得了更优的性能。然而，我们仍然观察到，该方法难以准确分割微小对象。未来，我们将探索如何使预训练模型适配高分辨率输入，以改善微小对象分割。此外，在图像级图像-文本对上预训练的冻结 Stable Diffusion 可用于改进分割，这表明探索更好的图像-文本预训练策略，尤其是面向高质量开放词汇分割的预训练策略，是一个很有前景的方向。
